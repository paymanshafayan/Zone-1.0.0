{
  "name": "@zone/db",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "prisma generate 2>/dev/null || echo 'prisma generate skipped (no network)' && tsc",
    "dev": "prisma generate 2>/dev/null || echo 'prisma generate skipped' && tsc --watch",
    "lint": "eslint src/",
    "test": "jest",
    "type-check": "prisma generate 2>/dev/null || echo 'prisma generate skipped (no network)' && tsc --noEmit",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.8.0"
  }
}
